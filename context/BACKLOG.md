# BACKLOG

Critical:

1. Keep runtime owner paths honest across `BeMore-stack`, `openclaw`, and `prismtek-site`.
2. Make BMO usable as a real public chat surface on `prismtek.dev` through the correct `prismtek-site` owner path.
3. Keep GitHub automation, workspace sync, and launchd/runtime docs aligned with the live scripts and workflows.
4. Keep Telegram delivery and `/exec` validation tied to the real `openclaw` runtime owner path.
5. Keep donor carryover from `PrismBot` and `omni-bmo` explicit, testable, and current.

Important:

6. Expand worker status, reseed, and snapshot tooling so operators can recover quickly without reading half the repo.
7. Refresh stale top-level docs whenever commands, targets, or runtime ownership change.
8. Tighten runtime-validation and delivery-contract proof paths for live smoke checks.
9. Keep repo-local skills discoverable, callable, and current with the actual operator workflows.
10. Track public-web handoff gaps for `prismtek.dev` so BMO can move from documented handoff to real web chat behavior.

Nice-to-have:

11. Easier worker auth for OpenClaw TUI and related local tooling.
12. Better cross-platform smoke coverage for Bash-heavy operator scripts.
13. More automated drift detection between repo docs, Makefile targets, and runtime scripts.
