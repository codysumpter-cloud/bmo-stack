# Local MCP Servers

This directory holds repo-local MCP servers and related operator tooling for `BeMore-stack`.

## Available servers

- `codex-bridge/` — dispatches Codex CLI work into isolated git worktrees and stores structured run artifacts under `runtime/runs/`.
