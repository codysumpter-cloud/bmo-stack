name: Build & TestFlight (moved)

on:
  workflow_dispatch:

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - name: Stop stale release lane
        run: |
          echo "::error title=Pipeline moved::BeMore iOS/TestFlight releases now ship from codysumpter-cloud/prismtek-apps via .github/workflows/bemore-ios-ci-testflight.yml."
          exit 1
